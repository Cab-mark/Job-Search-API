# Local development environment variables
# These settings override the defaults in app/config.py

# OpenSearch Configuration (for local uvicorn development)
# When running via docker-compose, use OPENSEARCH_HOST=opensearch
OPENSEARCH_HOST=localhost
OPENSEARCH_PORT=9200
OPENSEARCH_INDEX=jobs
