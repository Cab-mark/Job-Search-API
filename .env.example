# Job Search API - Environment Configuration
# Copy this file to .env and adjust values as needed

# OpenSearch Configuration
OPENSEARCH_HOST=opensearch
OPENSEARCH_PORT=9200
OPENSEARCH_INDEX=jobs

# Optional: OpenSearch Authentication (for production)
# OPENSEARCH_USERNAME=admin
# OPENSEARCH_PASSWORD=admin

# OpenSearch Dashboards (optional)
OPENSEARCH_DASHBOARDS_HOST=opensearch-dashboards
OPENSEARCH_DASHBOARDS_PORT=5601

# API Configuration (optional overrides)
# API_TITLE=Job Search API
# API_VERSION=1.0.0
# API_DESCRIPTION=FastAPI-based Job Search API with OpenSearch backend

# Pagination defaults (optional)
# DEFAULT_PAGE_SIZE=10
# MAX_PAGE_SIZE=100
